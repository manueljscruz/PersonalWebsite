class gitrepos{

    constructor(repoName, repoDescription, repoUrl, repoLanguage){
        this._repoName = repoName;
        this._repoDescription = repoDescription;
        this._repoUrl = repoUrl;
        this._repoLanguage = repoLanguage;
    }
}